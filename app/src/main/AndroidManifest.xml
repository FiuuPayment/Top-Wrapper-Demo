<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-sdk
    tools:overrideLibrary=
    "my.com.softspace.reader,
    my.com.softspace.ssfasstapsdk,
    my.com.softspace.sspog"
    />

    <uses-permission android:name="com.samsung.android.teelicense" />
    <uses-feature android:name="android.hardware.telephony" android:required="false" />
    <uses-feature android:name="android.hardware.nfc" android:required="false" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.NFC" />
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name="com.fiuu.toppayment.SDKTesterApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:extractNativeLibs="true">
        <activity android:name="com.fiuu.toppayment.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.fiuu.toppayment.SuccessPayment"
            android:exported="false">
        </activity>
        <activity android:name="com.fiuu.toppayment.FailPayment"
            android:exported="false">
        </activity>
        <activity android:name="com.fiuu.toppayment.SignatureActivity"
            android:exported="false">
        </activity>
    </application>

</manifest>